# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# pagefind

public/pagefind
node_modules

# oh-my-claudecode state
.omc/

# raw drafts (not for publishing)
raw/

# spotify token cache (secret) — written by scripts/spotify-playlist.py (spotipy)
# WARNING: this file is still TRACKED and already pushed to origin/master, so this
# rule does nothing for it yet. required manual steps, in order:
#   1. revoke the app at https://www.spotify.com/account/apps (refresh tokens never expire)
#   2. git rm --cached .cache
#   3. purge from history: git filter-repo --path .cache --invert-paths, then force-push
.cache
